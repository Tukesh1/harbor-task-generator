runs/
# Cross-run negative-example corpus (DATASET_ROOT) — generated by lib/dataset.capture_break_failure,
# accumulates per-run; not version-controlled. Track deliberately if clones should share it.
datasets/
**/__pycache__/
*.pyc
.venv/
venv/
.DS_Store
.env
.env.*
!.env.example
# Personal working notes — not part of the shipped pipeline.
notes/
